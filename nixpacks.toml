[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmds = ["npm install --production --silent --no-audit --no-fund", "cd client && npm install --silent --no-audit --no-fund"]

[phases.build]
cmds = ["cd client && npm run build", "node verify-build.js"]

[start]
cmd = "node railway-full-server.js"

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "true"
NPM_CONFIG_AUDIT = "false"
NPM_CONFIG_FUND = "false"
NPM_CONFIG_LOGLEVEL = "error"
