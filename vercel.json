{
  "version": 2,
  "installCommand": "npm install && cd loverble && npm install",
  "buildCommand": "cd loverble && npm run build && cd .. && node scripts/vercel-merge-output.mjs",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  }
}
